import React from 'react';
import { View, Text, SafeAreaView } from 'react-native';

const Chat = () => {
    return (
        <SafeAreaView style={{ flex: 1 }}>
            <Text>Ola</Text>
        </SafeAreaView>
    );
}

export default Chat;